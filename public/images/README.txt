Put your hero photo here as: hero.jpg
(temple + taxi photo, similar to the reference design)

Recommended: a wide/landscape photo, at least 1600px wide, so it looks
sharp on large screens. JPG or PNG both work — if you use a PNG, either
rename it to hero.jpg or update the src path in components/Hero.tsx to
match (e.g. "/images/hero.png").
